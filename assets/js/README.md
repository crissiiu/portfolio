# JavaScript Modules

JavaScript is written in a modular way to avoid global scope pollution.

## Files
- `main.js`: Application entry point and initialization
- `navigation.js`: Navigation menu and scrolling behavior
- `interaction.js`: UI interactions (buttons, animations, events)

All scripts are loaded using `defer` for better performance.